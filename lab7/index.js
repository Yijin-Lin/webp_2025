var countainer=document.getElementById("Container");
window.addEventListener("keyup",function(e){
    console.log(e.key);
    if(e.key){
    //檢查第一個字元是否為e.key
        first=countainer.innerHTML.charAt(0);
        if 
    //一樣的話把第一個字元消除
    }
    add_new_chars();
});

function add_new_chars(){
    //container亂數增加一或三個字元
}